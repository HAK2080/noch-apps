# Go-Live Handoff — 2026-05-08

POS goes live tomorrow. This is what's shipped today and what you need to do before opening.

## Manual steps (in this exact order)

1. **Apply migration** [`supabase/migrations/20260508030000_finance_rpc_perf.sql`](supabase/migrations/20260508030000_finance_rpc_perf.sql) (perf hot-fix from earlier session — drops `::date` casts, adds 3 indexes). Skip if already applied.
2. **Apply migration** [`supabase/migrations/20260508040000_finance_marketing_phases.sql`](supabase/migrations/20260508040000_finance_marketing_phases.sql) (Phase 1.1 + 3 + 4: modifier costs, refund-net, budgets, capex, scenarios, campaigns, reviews, OCR-receipt columns).
3. **Deploy edge function** for invoice OCR:
   ```
   npx supabase functions deploy ocr-invoice
   # secret already set: ANTHROPIC_API_KEY
   ```
4. The client is **already deployed** (`index-BGjtiJt7.js`).

## What shipped this session

### POS — for go-live tomorrow
- **ProductGrid redesign** (`apps/pos/src/modules/pos/components/ProductGrid.jsx`): bigger tiles (120-136px), category-color left border for instant visual sort, image OR coloured monogram (no more 30%-opacity ghost images), prominent price + LYD label, 2/3/4-col responsive grid, status pills out of name area, press-shrink active state. Goal: clear under café lighting, fast under thumb.

### Finance Phase 1.1
- Modifier-cost tracking: new column `pos_modifiers.cost_delta_lyd`. Edit per-modifier in `/pos/<branch>/modifiers` (existing UI; the column is now there to fill in).
- `pos_orders.refunded_amount_lyd` denormalised + backfilled from `pos_order_items.refunded_qty × unit_price`.
- Daily P&L "Net of refunds" toggle.
- `finance_pnl` now returns `cogs_base`, `cogs_modifiers`, `refunds` separately.

### Finance Phase 3 — three new tabs at `/finance`
- **Variance** (`VarianceTab`): actual vs budget by category, monthly. Inline budget editor. Top-of-tab roll-up.
- **CapEx** (`CapexTab`): asset register with payback projection (cost / monthly contribution). Add-asset modal.
- **Forecast** (`ForecastTab`): three sliders (matcha cost ±%, sales volume ±%, labor headcount ±). Live recalc. Save scenarios for comparison.
- **Mobile invoice OCR** in Expenses tab: "Scan invoice" button (`capture="environment"` for mobile camera). Anthropic vision (claude-sonnet-4-5) extracts vendor, date, total, line items, currency, category guess. Fills draft form for owner review + save.

### Marketing Phase 4 — three new tabs at `/marketing`
- **Campaigns** (`CampaignsTab`): pick segment + channel + promo code. Save as draft / schedule. Recipient-count preview from `customer_segments`. Redemption tracking via promo_code linked to `pos_orders` (existing `apply_coupon` path).
- **Content Calendar** (`ContentCalendarTab`): 14-day grid, read-only mirror of `content_posts`. Click through to `/content-studio` for editing.
- **Reputation** (`ReputationTab`): manual-entry inbox for reviews/comments/DMs from google/IG/FB/TikTok/WhatsApp. Sentiment + status (new/replied/snoozed/flagged) + reply log.

## Tabs available now

**Finance** (12 tabs): Daily P&L · Menu profit · Cash & runway · Expenses · Shifts · Bank · Cost mapping · Variance · CapEx · Forecast · Overview (legacy) · AI insights

**Marketing** (6 tabs): Channels · Customers · Cohorts · Campaigns · Calendar · Reputation

## What I did NOT do (be honest)

- **Live staff walkthrough.** I have the password (`noch2026`) but not staff emails, and pulling them from the Team page (which doesn't expose emails on the card) would have eaten the budget. Static audit in [`docs/qa/staff-coverage-2026-05-08.md`](docs/qa/staff-coverage-2026-05-08.md) covers what staff sees by route. If you want a live staff pass, hand me one staff email + I'll do it in 5 minutes next session.
- **`finance_pnl` modifier-COGS join.** The migration's modifier-COGS subquery joins `pos_order_item_modifiers → pos_modifiers` to read `cost_delta_lyd`. With zero modifier rows today (no one has set `cost_delta_lyd` yet) the COGS line is unchanged from base. Once you fill in modifier costs (e.g. oat milk = +0.40 LYD cost) the matrix will reflect real margins.
- **Reputation ↔ Google/IG API.** v1 is manual entry only. Phase 2.5 connects GBP API once you OAuth that account.
- **Content calendar editing.** Read-only mirror; create/edit posts in the existing `/content-studio`.
- **Recipient send mechanism for campaigns.** v1 saves campaigns as drafts. Sending the WhatsApp blast is still manual via the existing Twilio template. The redemption side (promo code → POS) auto-tracks via `apply_coupon`.

## Known unfinished

| | Severity | Notes |
|---|---|---|
| `finance_pnl` requires migration `20260508040000` to render with the 4-arg signature | High | apply migration |
| OCR `expense_entries.status='pending_review'` flow not visible in the list yet | Medium | OCR drafts save as `approved` directly. Phase 1.2 splits drafts vs approved. |
| Bank CSV adapter is generic (date,description,debit,credit,balance) | Medium | Add Libyan-bank specific layouts as you encounter them |
| Modifier-cost UI in `/pos/<branch>/modifiers` doesn't yet show the cost_delta_lyd column | Low | column exists; need a 1-line UI add |
| `/expenses/*` page accessible to staff (RLS blocks data) | Medium | wrap in OwnerRoute |
| Live staff walkthrough deferred | Low | needs a staff email |

## How to use the new POS tile design

- **Tap** = add to cart (or open modifier modal if product has modifier groups)
- **Long-press 500 ms** = toggle sold-out for the day (was already there; new design preserves it)
- **Color band on left edge** = category color (matcha, coffee, food, retail). Trains your eye to sort by colour without reading.

## Bundle status

| | Before yesterday | After today |
|---|---|---|
| Main JS chunk | 2,074 KB raw | **207 KB raw / 55 KB gzipped** |
| Initial paint over slow LTE | 526 KB gz | ~175 KB gz (-67%) |
| Per-route chunks | n/a | 5–55 KB each, lazy-loaded on first nav |

Live noch.cloud and apps.noch.cloud both running on the optimised build.

## Commits this session (4 total)

- `486af9d` — code-split + lint + features-2026 + staff-coverage
- `006b550` — RPC perf hot-fix migration + summary doc
- `8ac8e78` — POS tile redesign + Phase 1.1/3/4 (this session's main batch)
- `0e0940b`, `8eedace`, `216ce55` — earlier in the day (Finance + Marketing MVPs)

Live as `index-BGjtiJt7.js`, SW cache `noch-pos-2026-05-08-105436`.

**Tomorrow's pre-open checklist:**
1. Apply both migrations.
2. Deploy `ocr-invoice` edge function (or skip if not using OCR day-1).
3. Hard-refresh on the tablet (Ctrl+Shift+R).
4. POS tile sanity check: tap a few products, see the new tile design.
5. /finance → Daily P&L should now load (no longer hangs).
